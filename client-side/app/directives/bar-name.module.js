define(function (require) {

    "use strict";

    const angular = require("lib/angular");

    // Define the `barNameDirective` module
    return angular.
        module('barNameDirective', []);
});