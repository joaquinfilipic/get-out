define(function (require) {

    "use strict";

    const angular = require("lib/angular");

    // Define the `descriptionDirective` module
    return angular.
        module('descriptionDirective', []);
        
});