define(function (require) {

    "use strict";

    const angular = require("lib/angular");

    // Define the `barPriceDirective` module
    return angular.
        module('barPriceDirective', []);
});