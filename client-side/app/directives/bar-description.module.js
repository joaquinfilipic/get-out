define(function (require) {

    "use strict";

    const angular = require("lib/angular");

    // Define the `barDescriptionDirective` module
    return angular.
        module('barDescriptionDirective', []);
});