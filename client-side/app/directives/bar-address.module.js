define(function (require) {

    "use strict";

    const angular = require("lib/angular");

    // Define the `barAddressDirective` module
    return angular.
        module('barAddressDirective', []);
});