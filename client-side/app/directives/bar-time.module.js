define(function (require) {

    "use strict";

    const angular = require("lib/angular");

    // Define the `barTimeDirective` module
    return angular.
        module('barTimeDirective', []);
});